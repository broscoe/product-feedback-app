import React from 'react';


export default function 
Card({ suggestion, children }) {
    <div className = "card">
        <h3>{suggestion.feedback_title}</h3>
        <p>{suggestion.feedback_details}</p>
        <button>{suggestions.category}</button>
    </div>
}